server:
  port: 10086
spring:
  application:
    name: registry
eureka:
  client:
    service-url:
      defaultZone: http:172.0.0.1:10086/eureka
    fetch-registry: false #不拉取
    register-with-eureka: false #不注册